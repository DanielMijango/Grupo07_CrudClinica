<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Campo para ingresar el ID de la factura -->
        <EditText
            android:id="@+id/edtFacturaIDEliminar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Ingrese ID de la Factura"
            android:inputType="text" />

        <!-- Botón para eliminar la factura -->
        <Button
            android:id="@+id/btnEliminarFactura"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Eliminar Factura"
            android:layout_marginTop="12dp" />

        <!-- Mensaje de confirmación o error -->
        <TextView
            android:id="@+id/txtEliminarResultado"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Resultado de la eliminación aparecerá aquí"
            android:paddingTop="16dp"
            android:textSize="16sp" />

    </LinearLayout>
</ScrollView>
