<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Campo para ingresar la fecha -->
        <EditText
            android:id="@+id/edtBuscarFecha"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Ingrese Fecha (YYYY-MM-DD)"
            android:inputType="date" />

        <!-- Botón para realizar la búsqueda -->
        <Button
            android:id="@+id/btnBuscarFacturaPorFecha"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Buscar Factura"
            android:layout_marginTop="12dp" />

        <!-- Resultado de la búsqueda -->
        <TextView
            android:id="@+id/txtResultadoFactura"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Resultado aparecerá aquí"
            android:paddingTop="16dp"
            android:textSize="16sp" />

        <ListView
            android:id="@+id/listaDetallesFactura"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:divider="@android:color/darker_gray"
            android:dividerHeight="1dp"
            />

    </LinearLayout>
</ScrollView>
